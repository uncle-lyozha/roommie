export enum taskStatus {
    new = "new",
    pending = "pending",
    snoozed = "snoozed",
    done = "done",
    failed = "failed",
}

export enum moveEnum {
    forward = "forward",
    backwards = "backwards"
}

export enum editMenuOption {
    moveUserFwd = "moveFwd",
    moveUserBck = "moveBck",
    addUser = "addUser",
    delUser = "delUser",
    renameJob = "renameJob",
    editDescr = "editDescr"
}